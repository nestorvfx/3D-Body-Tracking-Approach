"""External 3D pose benchmark evaluators.  Each benchmark is a Python
module in this package that exposes a `build_benchmark(data_root)` function
returning an object implementing the Benchmark protocol in `base.py`.
"""
